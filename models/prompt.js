//세션에 저장될 prompt를 구성하는 모듈
//여기에 정의된 prompt를 조합해서 그때그때 사용함
//세션에 완성된 prompt를 담아두지 않는것은 서버자원 사용을 줄이기 위한 시도


class PromptGenerator{
    constructor(){
        this.defaultPrompt = ``
    }
    addPrompt();
    
}

prompt = new Prompt()

prompt.register('')


PromptRegister.addCart(prompt, order)
